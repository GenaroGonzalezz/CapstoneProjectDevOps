version: '3'
services:
    myapp:
      build: ./capstone-app
      command: npm start
      container_name: app_c
      ports:
        - '3000:3000'
    # volumes:
    # - ./hello-world:/app
    # - ./app/node_modules
      # stdin_open: true
      # tty: true
